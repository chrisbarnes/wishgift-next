import React from "react";

const GiftCardEdit = ({ handleEditGiftToggle }) => {
  return (
    <div>
      gift card edit <button onClick={handleEditGiftToggle}>cancel</button>
    </div>
  );
};

export default GiftCardEdit;
